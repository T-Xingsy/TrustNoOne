#!/bin/bash
# Promise Breaker CLI 启动脚本

# 获取脚本所在目录
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# 激活 conda 环境并运行 Python 脚本
conda activate hackason && python "$SCRIPT_DIR/main.py" "$@"
